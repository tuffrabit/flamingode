### Chat Completions Messages

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Retrieves chat messages for a given completion ID.

```APIDOC
## GET /chat/completions/{completion_id}/messages

### Description
Retrieves a list of messages associated with a specific chat completion.

### Method
GET

### Endpoint
/chat/completions/{completion_id}/messages

### Parameters
#### Path Parameters
- **completion_id** (string) - Required - The unique identifier for the chat completion.
```

--------------------------------

### Chat Completion Message

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Represents a chat completion message generated by the model.

```APIDOC
## ChatCompletionMessage object

A chat completion message generated by the model.

### Properties
- **content** (string) - Required - The contents of the message.
- **refusal** (string) - Optional - The refusal message generated by the model.
- **role** (string) - Required - The role of the author of this message. Must be `assistant`.
- **annotations** (array) - Optional - Annotations for the message, when applicable.
  - **type** (string) - Required - The type of the annotation. Always `url_citation`.
  - **url_citation** (object) - Required if type is `url_citation` - A URL citation.
    - **end_index** (number) - Required - The index of the last character of the URL citation.
    - **start_index** (number) - Required - The index of the first character of the URL citation.
    - **title** (string) - Required - The title of the URL citation.
    - **url** (string) - Required - The URL.
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Endpoints for managing chat completions, including creation, retrieval, updates, and deletion.

```APIDOC
## Chat Completions API

### Description
Provides endpoints for interacting with chat models to generate responses based on a conversation history.

### Method
POST

### Endpoint
/chat/completions

### Parameters
#### Request Body
- **messages** (array) - Required - A list of message objects representing the conversation.
- **model** (string) - Optional - The model to use for generating the completion.
- **temperature** (number) - Optional - Controls randomness. Lower values make the output more focused and deterministic.
- **stream** (boolean) - Optional - Whether to stream back partial message deltas as they are generated.

### Request Example
```json
{
  "messages": [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Who won the world series in 2020?"}
  ],
  "model": "gpt-3.5-turbo"
}
```

### Response
#### Success Response (200)
- **choices** (array) - A list of completion choices.
- **usage** (object) - An object containing token usage statistics.

#### Response Example
```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "The Los Angeles Dodgers won the World Series in 2020."
      }
    }
  ],
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 15,
    "total_tokens": 25
  }
}
```
```

```APIDOC
## List Chat Completions

### Description
Retrieves a list of chat completions.

### Method
GET

### Endpoint
/chat/completions

### Parameters
#### Query Parameters
- **limit** (integer) - Optional - The maximum number of completions to return.
- **order** (string) - Optional - The order in which to return completions (e.g., 'asc', 'desc').

### Response
#### Success Response (200)
- **data** (array) - A list of chat completion objects.
- **has_more** (boolean) - Indicates if there are more results available.
```

```APIDOC
## Get Chat Completion

### Description
Retrieves a specific chat completion by its ID.

### Method
GET

### Endpoint
/chat/completions/{completion_id}

### Parameters
#### Path Parameters
- **completion_id** (string) - Required - The unique identifier of the completion.

### Response
#### Success Response (200)
- **id** (string) - The unique identifier of the completion.
- **object** (string) - The type of object returned, should be 'chat.completion'.
- **created** (integer) - Unix timestamp of when the completion was created.
- **choices** (array) - A list of completion choices.
- **usage** (object) - An object containing token usage statistics.
```

```APIDOC
## Update Chat Completion

### Description
Updates a specific chat completion by its ID. Note: This functionality might be limited or specific to certain use cases.

### Method
POST

### Endpoint
/chat/completions/{completion_id}

### Parameters
#### Path Parameters
- **completion_id** (string) - Required - The unique identifier of the completion.

#### Request Body
- **status** (string) - Required - The new status for the completion (e.g., 'completed', 'failed').

### Response
#### Success Response (200)
- **id** (string) - The unique identifier of the completion.
- **status** (string) - The updated status of the completion.
```

```APIDOC
## Delete Chat Completion

### Description
Deletes a specific chat completion by its ID.

### Method
DELETE

### Endpoint
/chat/completions/{completion_id}

### Parameters
#### Path Parameters
- **completion_id** (string) - Required - The unique identifier of the completion.

### Response
#### Success Response (200)
- **id** (string) - The unique identifier of the deleted completion.
- **deleted** (boolean) - True if the completion was successfully deleted.
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/api-reference/chat

Endpoints for managing chat completions, including creation, retrieval, updates, and deletion.

```APIDOC
## POST /chat/completions

### Description
Creates a chat completion by providing a list of messages comprising a conversation.

### Method
POST

### Endpoint
/chat/completions

### Request Body
(Not specified in the provided text)

### Response
(Not specified in the provided text)
```

```APIDOC
## GET /chat/completions

### Description
Lists chat completions.

### Method
GET

### Endpoint
/chat/completions

### Query Parameters
(Not specified in the provided text)

### Response
(Not specified in the provided text)
```

```APIDOC
## GET /chat/completions/{completion_id}

### Description
Retrieves a specific chat completion by its ID.

### Method
GET

### Endpoint
/chat/completions/{completion_id}

### Path Parameters
- **completion_id** (string) - Required - The ID of the completion to retrieve.

### Response
(Not specified in the provided text)
```

```APIDOC
## POST /chat/completions/{completion_id}

### Description
Updates a chat completion.

### Method
POST

### Endpoint
/chat/completions/{completion_id}

### Path Parameters
- **completion_id** (string) - Required - The ID of the completion to update.

### Request Body
(Not specified in the provided text)

### Response
(Not specified in the provided text)
```

```APIDOC
## DELETE /chat/completions/{completion_id}

### Description
Deletes a chat completion.

### Method
DELETE

### Endpoint
/chat/completions/{completion_id}

### Path Parameters
- **completion_id** (string) - Required - The ID of the completion to delete.

### Response
(Not specified in the provided text)
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Endpoints for managing chat completions, including creation, retrieval, updates, and deletion.

```APIDOC
## Chat Completions API

### Description
Endpoints for managing chat completions, including creation, retrieval, updates, and deletion.

### Method
POST
### Endpoint
/chat/completions

### Description
Given a list of messages comprising a conversation, the model will return a response.

### Method
GET
### Endpoint
/chat/completions

### Description
Lists chat completions.

### Method
GET
### Endpoint
/chat/completions/{completion_id}

### Description
Retrieves a specific chat completion by its ID.

### Method
POST
### Endpoint
/chat/completions/{completion_id}

### Description
Updates an existing chat completion.

### Method
DELETE
### Endpoint
/chat/completions/{completion_id}

### Description
Deletes a chat completion by its ID.
```

--------------------------------

### Manage Chat Completions Context

Source: https://developers.openai.com/api/docs/guides/migrate-to-responses

Store and manage conversation history manually for multi-turn interactions with Chat Completions. Ensure the `messages` array is updated with each turn.

```javascript
let messages = [
    { 'role': 'system', 'content': 'You are a helpful assistant.' },
    { 'role': 'user', 'content': 'What is the capital of France?' }
  ];
const res1 = await client.chat.completions.create({
  model: 'gpt-5',
  messages
});

messages = messages.concat([res1.choices[0].message]);
messages.push({ 'role': 'user', 'content': 'And its population?' });

const res2 = await client.chat.completions.create({
  model: 'gpt-5',
  messages
});
```

--------------------------------

### Chat Completion Deletion

Source: https://developers.openai.com/api/docs/api-reference/chat

Represents a deleted chat completion object.

```APIDOC
## Chat Completion Deletion

### ChatCompletionDeleted
- **id** (string) - Required - The ID of the chat completion that was deleted.
- **deleted** (boolean) - Required - Whether the chat completion was deleted.
- **object** (string) - Required - The type of object being deleted, always `chat.completion.deleted`.
```

--------------------------------

### Chat Completion Deletion Object

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Represents the response when a chat completion is deleted.

```APIDOC
## ChatCompletionDeleted object

Represents the deletion status of a chat completion.

### Properties
- **id** (string) - Required - The ID of the chat completion that was deleted.
- **deleted** (boolean) - Required - Indicates if the chat completion was successfully deleted.
- **object** (string) - Required - The type of object being deleted, always `chat.completion.deleted`.
```

--------------------------------

### Chat Completion Chunk Object

Source: https://developers.openai.com/api/docs/api-reference/chat

Represents a streamed chunk of a chat completion response.

```APIDOC
## Chat Completion Chunk Object

### Description
Represents a streamed chunk of a chat completion response returned by the model, based on the provided input.

### Response Body Structure
- **id** (string) - A unique identifier for the chat completion. Each chunk has the same ID.
- **choices** (array of object) - A list of chat completion choices. Can contain more than one element if `n` is greater than 1. Can also be empty for the last chunk if you set `stream_options: {"include_usage": true}`.
  - **delta** (object) - A chat completion delta generated by streamed model responses.
    - **content** (string) - Optional - The contents of the chunk message.
    - **function_call** (object) - Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
      - **arguments** (string) - Optional - The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function.
      - **name** (string) - Optional - The name of the function to call.
    - **refusal** (string) - Optional - The refusal message generated by the model.
    - **role** (string) - Optional - The role of the author of this message. One of: `developer`, `system`, `user`, `assistant`, `tool`.
    - **tool_calls** (array of object) - Optional - The tool calls generated by the model.
      - **index** (number) - The index of the tool call.
      - **id** (string) - Optional - The ID of the tool call.
      - **function** (object) - Optional - Details about the function call.
        - **arguments** (string) - Optional - The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function.
        - **name** (string) - Optional - The name of the function to call.
      - **type** (string) - Optional - The type of the tool. Currently, only `function` is supported.
  - **finish_reason** (string) - The reason the model stopped generating tokens. One of: `stop`, `length`, `tool_calls`, `content_filter`, `function_call` (deprecated).
  - **index** (number) - The index of the choice in the list of choices.
  - **logprobs** (object) - Optional - Log probability information for the choice.
    - **content** (array of object) - A list of message content tokens with log probability information.
      - **token** (string) - The token.
      - **bytes** (array of number) - A list of integers representing the UTF-8 bytes representation of the token. Can be `null` if there is no bytes representation for the token.
      - **logprob** (number) - The log probability of this token.
      - **top_logprobs** (array of object) - List of the most likely tokens and their log probability at this token position.
        - **token** (string) - The token.
        - **bytes** (array of number) - A list of integers representing the UTF-8 bytes representation of the token. Can be `null` if there is no bytes representation for the token.
        - **logprob** (number) - The log probability of this token.
    - **refusal** (string) - The refusal message generated by the model.
```

--------------------------------

### Chat Completion Chunk Object

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Represents a streamed chunk of a chat completion response.

```APIDOC
## Chat Completion Chunk Object

### Description
Represents a streamed chunk of a chat completion response returned by the model.

### Fields
- **id** (string) - A unique identifier for the chat completion. Each chunk shares the same ID.
- **choices** (array of object) - A list of chat completion choices. Can be empty for the last chunk if `stream_options: {"include_usage": true}` is set.
  - **delta** (object) - A chat completion delta generated by streamed model responses.
    - **content** (string) - Optional - The contents of the chunk message.
    - **function_call** (object) - Deprecated and replaced by `tool_calls`. The name and arguments of a function to be called.
      - **arguments** (string) - Optional - The arguments to call the function with, in JSON format. Note: The model may not always generate valid JSON.
      - **name** (string) - Optional - The name of the function to call.
    - **refusal** (string) - Optional - The refusal message generated by the model.
    - **role** (string) - Optional - The role of the author of this message. Possible values: `developer`, `system`, `user`, `assistant`, `tool`.
    - **tool_calls** (array of object) - Optional - Details about tool calls made by the model.
      - **index** (number) - The index of the tool call.
      - **id** (string) - Optional - The ID of the tool call.
      - **function** (object) - Optional - Information about the function call.
        - **arguments** (string) - Optional - The arguments to call the function with, in JSON format. Note: The model may not always generate valid JSON.
        - **name** (string) - Optional - The name of the function to call.
      - **type** (string) - Optional - The type of the tool. Currently, only `function` is supported.
  - **finish_reason** (string) - The reason the model stopped generating tokens. Possible values: `stop`, `length`, `tool_calls`, `content_filter`, `function_call` (deprecated).
  - **index** (number) - The index of the choice in the list of choices.
  - **logprobs** (object) - Optional - Log probability information for the choice.
    - **content** (array of object) - A list of message content tokens with log probability information.
      - **token** (string) - The token.
      - **bytes** (array of number) - UTF-8 bytes representation of the token. Can be `null`.
      - **logprob** (number) - The log probability of this token.
      - **top_logprobs** (array of object) - List of the most likely tokens and their log probability.
        - **token** (string) - The token.
        - **bytes** (array of number) - UTF-8 bytes representation of the token. Can be `null`.
        - **logprob** (number) - The log probability of this token.
    - **refusal** (string) - Optional - Log probability information related to a refusal.
```

--------------------------------

### Manage Chat Completions Context in Python

Source: https://developers.openai.com/api/docs/guides/migrate-to-responses

Store and manage conversation history manually for multi-turn interactions with Chat Completions in Python. Ensure the `messages` list is updated with each turn.

```python
messages = [
    {"role": "system", "content": "You are a helpful assistant."}, 
    {"role": "user", "content": "What is the capital of France?"}
]
res1 = client.chat.completions.create(model="gpt-5", messages=messages)

messages += [res1.choices[0].message]
messages += [{"role": "user", "content": "And its population?"}]

res2 = client.chat.completions.create(model="gpt-5", messages=messages)
```

--------------------------------

### Chat Completion Parameters

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Details on parameters that can be used with chat completion requests.

```APIDOC
## Chat Completion Parameters

### Description
This section details various parameters and response structures related to chat completion requests.

### Parameters

#### Request Parameters

- **service_tier** (string) - Optional - When set, the response body will include the `service_tier` value based on the processing mode actually used to serve the request. This response value may be different from the value set in the parameter. Possible values: "auto", "default", "flex", "scale", "priority".
- **system_fingerprint** (string) - Optional - This fingerprint represents the backend configuration that the model runs with. Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.

#### Response Fields

- **usage** (CompletionUsage) - Optional - Usage statistics for the completion request. Includes `completion_tokens`, `prompt_tokens`, `total_tokens`, and 2 more fields.

### Response Example
```json
{
  "service_tier": "auto",
  "usage": {
    "completion_tokens": 100,
    "prompt_tokens": 50,
    "total_tokens": 150
  }
}
```
```

--------------------------------

### Chat Completions API Example

Source: https://developers.openai.com/api/docs/guides/responses-vs-chat-completions

Use this snippet to generate text using the Chat Completions API. Ensure you have the 'openai' library installed and are authenticated.

```python
from openai import OpenAI
client = OpenAI()

completion = client.chat.completions.create(
model="gpt-5",
messages=[
{
"role": "user",
"content": "Write a one-sentence bedtime story about a unicorn."
}
]
)

print(completion.choices[0].message.content)
```

--------------------------------

### Chat Completion Deletion

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Represents the response object for a deleted chat completion.

```APIDOC
## ChatCompletionDeleted object

Represents the response object for a deleted chat completion.

### Properties
- **id** (string) - Required - The ID of the chat completion that was deleted.
- **deleted** (boolean) - Required - Whether the chat completion was deleted.
- **object** (string) - Required - The type of object being deleted. Always `chat.completion.deleted`.
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/models/chatgpt-4o-latest

Endpoint for interacting with chat-based models.

```APIDOC
## POST /v1/chat/completions

### Description
Endpoint for interacting with chat-based models to generate responses.

### Method
POST

### Endpoint
/v1/chat/completions

### Parameters
#### Request Body
- **model** (string) - Required - The model to use for chat completions.
- **messages** (array) - Required - A list of messages comprising the conversation so far.
- **temperature** (number) - Optional - Controls randomness. Lower values make the output more focused and deterministic.
- **max_tokens** (integer) - Optional - The maximum number of tokens to generate in the completion.

### Request Example
```json
{
  "model": "gpt-4o",
  "messages": [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Who won the world series in 2020?"}
  ],
  "temperature": 0.7,
  "max_tokens": 150
}
```

### Response
#### Success Response (200)
- **choices** (array) - An array of completion choices.
  - **message** (object) - The message content.
    - **role** (string) - The role of the author (e.g., 'assistant').
    - **content** (string) - The content of the message.
- **usage** (object) - Usage statistics for the request.
  - **prompt_tokens** (integer) - Number of tokens in the prompt.
  - **completion_tokens** (integer) - Number of tokens in the completion.
  - **total_tokens** (integer) - Total tokens used.

#### Response Example
```json
{
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "The Los Angeles Dodgers won the World Series in 2020."
      }
    }
  ],
  "usage": {
    "prompt_tokens": 25,
    "completion_tokens": 15,
    "total_tokens": 40
  }
}
```
```

--------------------------------

### Generate Text with Chat Completions (cURL)

Source: https://developers.openai.com/api/docs/guides/migrate-to-responses

Make a cURL request to the Chat Completions endpoint to generate text. Include Content-Type and Authorization headers, and provide model and messages in the request body.

```bash
curl https://api.openai.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $OPENAI_API_KEY" \
  -d '{
      "model": "gpt-5",
      "messages": [
          {"role": "system", "content": "You are a helpful assistant."},
          {"role": "user", "content": "Hello!"}
      ]
  }'
```

--------------------------------

### Chat Completion Chunk Object

Source: https://developers.openai.com/api/docs/api-reference/chat/object

Represents a chunk of a chat completion response, including token log probabilities and other metadata.

```APIDOC
## Chat Completion Chunk Object

### Description
Represents a chunk of a chat completion response. Each chunk contains information about the generated tokens, including their log probabilities and other metadata.

### Fields
- **id** (string) - The ID of the chat completion.
- **choices** (array of ChatCompletionChunkChoice) - A list of chat completion choices. Each choice contains a delta, the index of the choice, and potentially finish reasons.
- **created** (number) - The Unix timestamp (in seconds) of when the chat completion was created. Each chunk has the same timestamp.
- **model** (string) - The model used to generate the completion.
- **object** ("chat.completion.chunk") - The object type, which is always `chat.completion.chunk`.
- **system_fingerprint** (optional string) - This fingerprint represents the backend configuration that the model runs with. Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
- **usage** (optional CompletionUsage) - An optional field that will only be present when you set `stream_options: {"include_usage": true}` in your request. When present, it contains a null value **except for the last chunk** which contains the token usage statistics for the entire request.

### Nested Objects
#### ChatCompletionTokenLogprob
- **token** (string) - The token.
- **bytes** (optional array of number) - A list of integers representing the UTF-8 bytes representation of the token. Can be `null` if there is no bytes representation for the token.
- **logprob** (number) - The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely.
- **top_logprobs** (array of object) - List of the most likely tokens and their log probability, at this token position. In rare cases, there may be fewer than the number of requested `top_logprobs` returned.
  - **token** (string) - The token.
  - **bytes** (optional array of number) - A list of integers representing the UTF-8 bytes representation of the token. Can be `null` if there is no bytes representation for the token.
  - **logprob** (number) - The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely.

#### ChatCompletionContentPart
Represents a part of the content in a chat message. Can be text, an image URL, or audio input.

**Types:**
- **ChatCompletionContentPartText**
  - **text** (string) - The text content.
  - **type** ("text") - The type of the content part.
- **ChatCompletionContentPartImage**
  - **image_url** (object)
    - **url** (string) - Either a URL of the image or the base64 encoded image data.
    - **detail** (optional string) - Specifies the detail level of the image. One of: "auto", "low", "high".
  - **type** ("image_url") - The type of the content part.
- **ChatCompletionContentPartInputAudio**
  - **input_audio** (object)
    - **data** (string) - Base64 encoded audio data.
    - **format** (string) - The format of the encoded audio data. Currently supports “wav” and “mp3”.
  - **type** ("input_audio") - The type of the content part.
- **FileContentPart**
  - **file** (object)
    - **file_data** (optional string) - The base64 encoded file data.
    - **file_id** (optional string) - The ID of the file.
    - **filename** (optional string) - The filename.
  - **type** (string) - The type of the content part.
```

--------------------------------

### GET /chat/completions/{completion_id}/messages

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Retrieves messages associated with a specific chat completion.

```APIDOC
## GET /chat/completions/{completion_id}/messages

### Description
Given a list of messages comprising a conversation, the model will return a response.

### Method
GET

### Endpoint
/chat/completions/{completion_id}/messages

### Parameters
#### Path Parameters
- **completion_id** (string) - Required - The ID of the chat completion.
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/models/gpt-image-2

Endpoints for interacting with chat-based language models.

```APIDOC
## POST /v1/chat/completions

### Description
Creates a completion for the chat message.

### Method
POST

### Endpoint
/v1/chat/completions

### Request Body
- **model** (string) - Required - ID of the model to use. See the model endpoint to get a list of all available models, or the Models API for more details.
- **messages** (array) - Required - A list of messages comprising the conversation so far. Each message is an object with a role and content.
  - **role** (string) - Required - The role of the author of a message. One of `system`, `user`, or `assistant`.
  - **content** (string) - Required - The content of the message.
- **temperature** (number) - Optional - Controls randomness. Lower values make the output more focused and deterministic. Defaults to 1.
- **max_tokens** (integer) - Optional - The maximum number of tokens to generate in the completion.

### Request Example
{
  "model": "gpt-3.5-turbo",
  "messages": [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Who won the world series in 2020?"}
  ]
}

### Response
#### Success Response (200)
- **id** (string) - A unique identifier for the completion.
- **choices** (array) - A list of completion choices.
  - **message** (object) - The message content.
    - **role** (string) - The role of the author.
    - **content** (string) - The content of the message.

#### Response Example
{
  "id": "chatcmpl-123",
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "The Los Angeles Dodgers won the World Series in 2020."
      }
    }
  ]
}
```

--------------------------------

### Chat Completion Chunk Object

Source: https://developers.openai.com/api/docs/api-reference/chat

Details the structure of a single chunk in a streaming chat completion response.

```APIDOC
## Chat Completion Chunk Object

This object represents a chunk of a chat completion response when streaming is enabled.

### Fields

- **id** (string) - Unique identifier for the chunk.
- **choices** (array) - A list of chat completion choices. Each choice contains:
  - **delta** (object) - The delta object, which contains the content of the message.
    - **role** (string) - The role of the author of this content (e.g., 'assistant').
    - **content** (string) - The content of the message.
  - **finish_reason** (string) - The reason the model stopped generating tokens (e.g., 'stop', 'length').
  - **index** (number) - The index of the choice in the array.
- **created** (number) - The Unix timestamp (in seconds) of when the chat completion was created. Each chunk has the same timestamp.
- **model** (string) - The model that generated the completion.
- **object**: "chat.completion.chunk" - The object type, which is always `chat.completion.chunk`.
- **system_fingerprint** (string, optional) - This fingerprint represents the backend configuration that the model runs with. Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
- **usage** (object, optional) - An optional field that will only be present when you set `stream_options: {"include_usage": true}` in your request. When present, it contains a null value **except for the last chunk** which contains the token usage statistics for the entire request.
  - **completion_tokens** (number) - Number of tokens in the completion.
  - **prompt_tokens** (number) - Number of tokens in the prompt.
  - **total_tokens** (number) - Total tokens in the request.

### Response Example

```json
{
  "id": "chatcmpl-123",
  "choices": [
    {
      "delta": {
        "role": "assistant",
        "content": "The quick brown fox"
      },
      "finish_reason": null,
      "index": 0
    }
  ],
  "created": 1677652288,
  "model": "gpt-4",
  "object": "chat.completion.chunk",
  "system_fingerprint": "fp_12345",
  "usage": null
}
```
```

--------------------------------

### Completions (Legacy) API

Source: https://developers.openai.com/api/docs/models/gpt-5.5-pro

Legacy endpoint for text completions. Use Chat Completions API for newer applications.

```APIDOC
## POST v1/completions

### Description
Provides text completions. This is a legacy endpoint; consider using the Chat Completions API for new projects.

### Method
POST

### Endpoint
/v1/completions

### Parameters
#### Request Body
- **model** (string) - Required - The model to use for completions (e.g., "gpt-5.5-pro").
- **prompt** (string) - Required - The prompt to generate completions for.
- **max_tokens** (integer) - Optional - The maximum number of tokens to generate.

### Request Example
```json
{
  "model": "gpt-5.5-pro",
  "prompt": "Once upon a time"
}
```

### Response
#### Success Response (200)
- **choices** (array) - The generated text completions.
- **usage** (object) - Token usage information.
```

--------------------------------

### Chat Completions API

Source: https://developers.openai.com/api/docs/models/gpt-image-1-mini

Endpoint for engaging in chat conversations with the model. Supports streaming responses.

```APIDOC
## POST /v1/chat/completions

### Description
Creates a completion for the chat message. This endpoint is the primary way to interact with the model for conversational applications.

### Method
POST

### Endpoint
/v1/chat/completions

### Parameters
#### Request Body
- **model** (string) - Required - The model to use for the chat completion.
- **messages** (array) - Required - A list of messages comprising the conversation so far. Each message is an object with a `role` and `content`.
  - **role** (string) - Required - The role of the author of the message. Must be one of "system", "user", or "assistant".
  - **content** (string) - Required - The content of the message.
- **stream** (boolean) - Optional - Whether to stream back partial message deltas as they become available. Defaults to false.
- **temperature** (number) - Optional - Controls randomness. Lower values make the output more focused and deterministic. Defaults to 1.
- **max_tokens** (integer) - Optional - The maximum number of tokens to generate in the completion.

### Request Example
```json
{
  "model": "gpt-4",
  "messages": [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "Who won the world series in 2020?"}
  ],
  "stream": true
}
```

### Response
#### Success Response (200)
- **id** (string) - A unique identifier for the completion.
- **object** (string) - The type of object returned, usually "chat.completion".
- **created** (integer) - Unix timestamp of when the completion was created.
- **choices** (array) - A list of completion choices.
  - **message** (object) - The message content and role.
    - **role** (string) - The role of the author of the message.
    - **content** (string) - The content of the message.
  - **finish_reason** (string) - The reason the model stopped generating tokens.
- **usage** (object) - Usage statistics for the request.
  - **prompt_tokens** (integer) - Number of tokens in the prompt.
  - **completion_tokens** (integer) - Number of tokens in the completion.
  - **total_tokens** (integer) - Total tokens used.

#### Response Example
```json
{
  "id": "chatcmpl-123",
  "object": "chat.completion",
  "created": 1677652288,
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "The Los Angeles Dodgers won the World Series in 2020."
      },
      "finish_reason": "stop"
    }
  ],
  "usage": {
    "prompt_tokens": 10,
    "completion_tokens": 15,
    "total_tokens": 25
  }
}
```
```

--------------------------------

### Chat Completions API Function Definition

Source: https://developers.openai.com/api/docs/guides/responses-vs-chat-completions

Defines a function for the Chat Completions API. Note the 'strict' parameter and the nested 'function' object.

```json
{
      "type": "function",
      "function": {
        "name": "get_weather",
        "description": "Determine weather in my location",
        "strict": true,
        "parameters": {
          "type": "object",
          "properties": {
            "location": {
              "type": "string"
            }
          },
          "additionalProperties": false,
          "required": [
            "location",
            "unit"
          ]
        }
      }
  }
```

--------------------------------

### Chat Completion Content Parts

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Defines the structure for different types of content that can be included in chat completions, such as images, audio, text, and refusals.

```APIDOC
## ChatCompletionContentPartImage object

Represents an image input for chat completions.

### Properties
- **image_url** (object) - Required - Contains the URL or base64 encoded data of the image.
  - **url** (string) - Required - The URL of the image or base64 encoded image data.
  - **detail** (string) - Optional - Specifies the detail level of the image. Can be "auto", "low", or "high".
- **type** (string) - Required - Always `file`.

## ChatCompletionContentPartInputAudio object

Represents an audio input for chat completions.

### Properties
- **input_audio** (object) - Required - Contains the audio data and format.
  - **data** (string) - Required - Base64 encoded audio data.
  - **format** (string) - Required - The format of the encoded audio data. Currently supports `wav` and `mp3`.
- **type** (string) - Required - Always `input_audio`.

## ChatCompletionContentPartRefusal object

Represents a refusal message generated by the model.

### Properties
- **refusal** (string) - Required - The refusal message.
- **type** (string) - Required - Always `refusal`.

## ChatCompletionContentPartText object

Represents a text input for chat completions.

### Properties
- **text** (string) - Required - The text content.
- **type** (string) - Required - Always `text`.
```

--------------------------------

### Generate Text with Chat Completions (Python)

Source: https://developers.openai.com/api/docs/guides/migrate-to-responses

Generate text using Chat Completions in Python by defining messages with roles and content. The OpenAI client should be instantiated.

```python
from openai import OpenAI
client = OpenAI()

completion = client.chat.completions.create(
    model="gpt-5",
    messages=[
        {"role": "system", "content": "You are a helpful assistant."},
        {"role": "user", "content": "Hello!"}
    ]
)
print(completion.choices[0].message.content)
```

--------------------------------

### Chat Completion Message Objects

Source: https://developers.openai.com/api/docs/api-reference/chat/create

Defines the structure for messages within a chat completion, including assistant messages, tool calls, and content parts.

```APIDOC
## Chat Completion Message Objects

### Description
This section describes the various objects used to represent messages in chat completions, including assistant messages, tool calls, and content parts.

### Message Objects

#### ChatCompletionAssistantMessageParam

- **role**: "assistant" (string) - The role of the messages author.
- **audio**: object - Optional - Data about a previous audio response from the model.
  - **id**: (string) - Unique identifier for a previous audio response from the model.
- **content**: optional string or array of ChatCompletionContentPartText or ChatCompletionContentPartRefusal - The contents of the assistant message. Required unless `tool_calls` or `function_call` is specified.
  - **TextContent**: (string) - The contents of the assistant message.
  - **ArrayOfContentParts**: array of ChatCompletionContentPartText or ChatCompletionContentPartRefusal
    - **ChatCompletionContentPartText**
      - **text**: (string) - The text content.
      - **type**: "text" (string) - The type of the content part.
    - **ChatCompletionContentPartRefusal**
      - **refusal**: (string) - The refusal message generated by the model.
      - **type**: "refusal" (string) - The type of the content part.
- **function_call**: object - Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called.
  - **arguments**: (string) - The arguments to call the function with, as generated by the model in JSON format.
  - **name**: (string) - The name of the function to call.
- **name**: (string) - An optional name for the participant.
- **refusal**: (string) - The refusal message by the assistant.
- **tool_calls**: array of ChatCompletionMessageToolCall - The tool calls generated by the model.
  - **ChatCompletionMessageFunctionToolCall**
    - **id**: (string) - The ID of the tool call.
    - **function**: object
      - **arguments**: (string) - The arguments to call the function with.
      - **name**: (string) - The name of the function to call.
    - **type**: "function" (string) - The type of the tool.
  - **ChatCompletionMessageCustomToolCall**
    - **id**: (string) - The ID of the tool call.
    - **custom**: object
      - **input**: (string) - The input for the custom tool call.
      - **name**: (string) - The name of the custom tool to call.
    - **type**: "custom" (string) - The type of the tool.

### Request Example (Assistant Message with Text Content)
```json
{
  "role": "assistant",
  "content": "Hello! How can I help you today?"
}
```

### Request Example (Assistant Message with Tool Call)
```json
{
  "role": "assistant",
  "tool_calls": [
    {
      "id": "call_abc123",
      "type": "function",
      "function": {
        "name": "get_weather",
        "arguments": "{\"location\": \"Paris\"}"
      }
    }
  ]
}
```
```

--------------------------------

### Upload File and Get Chat Completion (Python)

Source: https://developers.openai.com/api/docs/guides/pdf-files

Use the OpenAI Python SDK to upload a file and then reference its ID when creating a chat completion.

```python
from openai import OpenAI
client = OpenAI()

file = client.files.create(
    file=open("draconomicon.pdf", "rb"),
    purpose="user_data"
)

completion = client.chat.completions.create(
    model="gpt-5",
    messages=[
        {
            "role": "user",
            "content": [
                {
                    "type": "file",
                    "file": {
                        "file_id": file.id,
                    }
                },
                {
                    "type": "text",
                    "text": "What is the first dragon in the book?",
                },
            ],
        }
    ]
)

print(completion.choices[0].message.content)
```
